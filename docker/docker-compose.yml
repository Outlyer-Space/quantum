name: quantum
services:
  mongo:
    image: mongo:6
    container_name: quantum-mongo
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"
    networks:
      - quantum-net

  quantum:
    build:
      context: ../node
      dockerfile: ../docker/Dockerfile
    image: xenon130/quantum
    container_name: quantum-app
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    environment:
      NODE_ENV: development
      MONGO_DB_URL: mongodb://mongo:27017/quantum
      MONGO_URL: mongodb://mongo:27017/quantum
      MONGO_DB_USR:
      MONGO_DB_PWD:
      AUTH_PROVIDER: Mongo
      AUTH_TENANT_ID:
      AUTH_CALLBACK_URL:
      AUTH_CLIENT_ID: sys.admin@localhost
      AUTH_CLIENT_SECRET: 2infinity
    volumes:
      - ../node:/node #This parameter needs to be adjusted in a Production setting 
      - node_modules:/node/node_modules
    ports:
      - "3000:3000"
    networks:
      - quantum-net

volumes:
  mongo-data:
  node_modules:

networks:
  quantum-net:
    driver: bridge
